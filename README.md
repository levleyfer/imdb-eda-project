data set is in https://datasets.imdbws.com/

This project performs exploratory data analysis (EDA) on the IMDb dataset to uncover trends in movies, including ratings, genres, and voting patterns using Python (Pandas, NumPy, Matplotlib).
